type TypePlayerData = {
  name: string; // name of player
  pointScore: number; // number of points won in current game, e.g. 0/1/2/3 (1 represents 15, 2 represents 30...)
  gameScore: number; // number of games won in this set
};

export default TypePlayerData;
